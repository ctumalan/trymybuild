import type { APIRoute } from 'astro';
import { currentUser, env } from '../../server/auth';
import { database, ensureMember } from '../../server/database';
import { isFounder } from '../../server/admin-policy.mjs';
import { surface, signIn, e } from '../../server/feedback-ui';

export const GET: APIRoute = async context => {
 const user = await currentUser(context);
 if (!user) return signIn('/dashboard/profile');
 const admin = isFounder(user.id, env('FOUNDER_WORKOS_USER_ID'));
 try {
  const member = await ensureMember(user);
  const {data:p,error} = await database().from('profiles').select('display_name,identity_label,bio,is_public,avatar_path,website,slug,creator_type,location,location_public').eq('user_id',member.id).single();
  if(error) throw error;
  return surface('Edit profile', `<header><h1>Edit profile</h1></header><section class="cw-panel profile-editor"><form id="profile-form"><fieldset><legend>Profile photo or avatar</legend><div class="profile-photo-row"><img id="avatar-preview" class="person-avatar large" src="${e(p.avatar_path || 'data:image/svg+xml,'+encodeURIComponent('<svg xmlns="http://www.w3.org/2000/svg" width="96" height="96"><rect width="96" height="96" fill="#dcebdd"/><text x="48" y="60" text-anchor="middle" font-size="36" fill="#17332a">'+e(p.display_name.trim().slice(0,1)||'M')+'</text></svg>'))}" alt="Your profile image"><div><input class="visually-hidden" id="photo" type="file" accept="image/jpeg,image/png,image/webp"><label class="secondary-button photo-upload-button" for="photo">Change photo</label><p class="cw-meta">JPG, PNG, or WebP, up to 5 MB. Photos are cropped to a square.</p></div></div><p class="cw-meta">Or choose an avatar</p><div class="avatar-choices">${['leaf','sun','wave','spark'].map(a=>`<button type="button" data-avatar="/assets/avatars/${a}.svg" aria-pressed="${p.avatar_path==='/assets/avatars/'+a+'.svg'}" aria-label="Choose ${a} avatar"><img src="/assets/avatars/${a}.svg" alt=""></button>`).join('')}<button type="button" data-avatar="" aria-pressed="${!p.avatar_path}">Use my initial</button></div></fieldset><label for="displayName">Your public name</label><input type="text" id="displayName" name="displayName" value="${e(p.display_name)}" maxlength="60" required><label for="label">How you describe yourself</label><input type="text" id="label" name="label" value="${e(p.identity_label||'')}" maxlength="60" placeholder="Musician, Parent, Engineer…"><label for="bio">A little about you</label><textarea id="bio" name="bio" maxlength="500">${e(p.bio||'')}</textarea><label for="creatorType">Creator type</label><select id="creatorType" name="creatorType"><option value="independent" ${p.creator_type==='independent'?'selected':''}>Independent</option><option value="company" ${p.creator_type==='company'?'selected':''}>Company</option></select><label for="location">City and country (optional)</label><input id="location" name="location" maxlength="100" value="${e(p.location||'')}" placeholder="San Francisco, USA"><label class="cw-choice"><input type="checkbox" name="locationPublic" ${p.location_public?'checked':''}>Show this location on my public profile</label><label for="website">Website (optional)</label><input id="website" name="website" type="url" maxlength="1000" value="${e(p.website)}" placeholder="https://your-site.com"><label class="cw-choice"><input type="checkbox" name="isPublic" ${p.is_public?'checked':''}>Make my profile public</label><div class="profile-save-row"><p id="profile-status" role="status" aria-live="polite"></p><button class="primary-button" type="submit">Save my profile</button></div></form></section><form action="/auth/sign-out" method="post"><button class="secondary-button" type="submit">Sign out</button></form><script src="/profile-editor.js" defer></script>`, 'profile',200,admin);
 } catch { return surface('Edit profile','<h1>Edit profile</h1><p role="alert">Your profile could not be loaded. Please try again.</p><a href="/dashboard/profile">Try again</a>','profile',503,admin); }
};
