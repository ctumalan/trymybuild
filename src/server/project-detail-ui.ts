import {e} from './feedback-ui';
export function projectDetailContent(p:any){
 const c=p.creator||{},note=c.founderException?'Founder exception—not earned through feedback. Not a product-quality guarantee.':c.verificationNote||'Qualifying contribution and app ownership confirmed';
 return `<section class="recipient-hero"><div class="recipient-copy"><p class="eyebrow">You’re invited to try something new</p><span class="recipient-category">${e(p.category)} · ${e(p.stage)}</span><h1>${e(p.presentation.headline||p.name)}</h1><p class="recipient-project-name">${e(p.name)}</p><div class="public-builder">${c.avatar?`<img src="${e(c.avatar)}" alt="">`:`<span class="person-avatar">${e(c.initials||'C')}</span>`}<div><strong>Built by ${e(c.name||'You')}</strong>${c.verified?`<span class="creator-verified" title="${e(note)}">✓ Verified Creator</span>`:''}<p>${e(c.label||'')}</p></div></div></div><div class="recipient-preview-column"><div class="recipient-art">${p.preview?`<img src="${e(p.preview)}" alt="Preview of ${e(p.name)}">`:''}<span>Made by a person. Ready for your perspective.</span></div>${p.url?`<a class="primary-button" href="${e(p.url)}" target="_blank" rel="noopener noreferrer" data-try-app="${e(p.slug)}">Try this project ↗</a>`:''}</div></section><section class="recipient-answers"><article><p class="eyebrow">Why try it?</p><h2>How it helps</h2><p>${e(p.presentation.help)}</p></article><article><p class="eyebrow">Start here</p><h2>One thing to try first</h2><p>${e(p.presentation.firstTry)}</p></article></section>`;
}
